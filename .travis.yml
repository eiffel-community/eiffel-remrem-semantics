dist: trusty

sudo: required
addons:
  apt:
    packages:
      - openjdk-17-jdk

language: java

jdk:
  - openjdk17
  
before_install:
  - uname -a
  - chmod +x pom.xml
  
script:
  - mvn clean test
